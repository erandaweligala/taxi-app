# Copy to .env and adjust as needed. Defaults target the docker-compose stack.
HTTP_ADDR=:8080
GATEWAY_ADDR=:8090
REDIS_ADDR=localhost:6379
KAFKA_BROKERS=localhost:9092
POSTGRES_DSN=postgres://taxi:taxi@localhost:5432/taxi?sslmode=disable
LOCATION_TOPIC=driver-locations
TRIP_TOPIC=trip-events
